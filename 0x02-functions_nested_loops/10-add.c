#include "main.h"
/**
 * add - prints thesum of two integers
 *@m: first parameter
 *@n: second parameter
 * Return: an int which is the sum of two given integers
 */
int add(int m, int n)
{
	int r;

	r = m + n;
	return (r);
}
