/**
 * _isupper : checks for uppercase character
 *
 * Return: returns 1 if a character is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
