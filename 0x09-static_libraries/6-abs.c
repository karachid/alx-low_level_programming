#include "main.h"
/**
 * _abs - returns the absolute value of a given int
 * @num: parameter
 * Return: Absolute value
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
